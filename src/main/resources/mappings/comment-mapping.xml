<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="CommentDAO">
	<select id="getCommentList" parameterType="int" resultType="comment">
		SELECT *
		FROM product_comment
		WHERE pseq=#{pseq}
		ORDER BY regdate DESC
	</select>
	
	<select id="countCommentList" parameterType="int" resultType="int">
		SELECT COUNT(*)
		FROM notice_comment
		WHERE nseq=#{nseq}
	</select>
	
	<select id="getCommentListWithPaging" parameterType="HashMap" resultType="noticeComment">
		<![CDATA[
		SELECT notice_comment_seq, nseq, content, writer, regdate, modifydate
		FROM (SELECT ROW_NUMBER() OVER(ORDER BY regdate) rn, notice_comment_seq, nseq, content, writer, regdate, modifydate
			FROM notice_comment
			WHERE nseq=#{nseq})
		WHERE rn <= #{criteria.pageNum} * #{criteria.rowsPerPage}
			AND rn > (#{criteria.pageNum} - 1) * #{criteria.rowsPerPage}
		]]>
	</select>
	
	<insert id="saveComment">
		INSERT INTO notice_comment(notice_comment_seq, nseq, content, writer)
		VALUES(notice_comment_seq.NEXTVAL, #{nseq}, #{content}, #{writer})
	</insert>
	
	<update id="updateComment">
		UPDATE product_comment
		SET content=#{content}, modifyDate=SYSDATE
		WHERE comment_seq=#{comment_seq}
	</update>
	
	<delete id="deleteComment" parameterType="int">
		DELETE
		FROM product_comment
		WHERE comment_seq=#{comment_seq}
	</delete>
</mapper>