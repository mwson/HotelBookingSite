<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="NoticeDAO">	
	<select id="listNotice" resultType="notice">
		SELECT *
		FROM notice
		ORDER BY nseq DESC
	</select>
	
	<!-- 공지사항 디테일 하단에 3개 표시 SQL -->
	<select id="listNoticeView" resultType="notice">
		SELECT *
		FROM list_notice_view
	</select>
	
	<select id="getNotice" parameterType="int" resultType="notice">
		SELECT *
		FROM notice
		WHERE nseq=#{nseq}
	</select>
	
	<insert id="insertNotice">
		INSERT INTO notice(bseq, kind, subject, content, image)
		VALUES(notice_seq.NEXTVAL, #{kind}, #{subject}, #{content}, #{image})
	</insert>
	
	<update id="updateNotice">
		UPDATE notice
		SET kind=#{kind}, subject=#{subject}, content=#{content}, image=#{image}
		WHERE nseq=#{nseq}
	</update>
	
	<delete id="deleteNotice" parameterType="int">
		DELETE FROM notice
		WHERE nseq=#{nseq}
	</delete>

</mapper>